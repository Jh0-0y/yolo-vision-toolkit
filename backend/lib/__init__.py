"""순수 기능 모음 — 값을 받아 값을 돌려주는 계산과 파일 조작.

`app/` 과 `infra/` 를 **절대 import 하지 않는다.** 프로세스도 DB도 HTTP도 잡도
모른다. 그래서 웹 서버 없이 테스트할 수 있고, CLI 나 배치 스크립트가 그대로
가져다 쓸 수 있다.

`lib/` 안의 모듈끼리도 서로 import 하지 않는다 — 연결은 호출자(파이프라인)가
데이터를 넘겨 주는 방식으로 한다.
"""
