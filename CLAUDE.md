# CLAUDE.md

@AGENTS.md

## Claude Code 전용 규칙

### 언어
- 사용자와의 대화, 커밋 메시지, `docs/agents/` 문서, 새로 쓰는 코드 주석·docstring 은 **한국어**.
- 식별자와 로그·예외 메시지는 영어. 자세한 것은 [명명 규칙](docs/agents/conventions/naming.md).

### 확인 없이 하지 않는 것
- **명시적 지시 없이 코드를 수정하지 않는다.** 애매한 동의("ㅇㅋ", "그렇네")는 수정 승인이 아니다 — 변경안을 제시하고 기다린다.
- 의존성 추가·제거·버전 변경 (`uv add` · `npm install` · `adaptive-crop` 태그)
- 파일·브랜치의 개명·이동·삭제
- 브랜치 생성·전환, PR 생성·병합
- `data/` 아래 실제 데이터를 지우거나 덮어쓰는 일

### 금지
- `git reset --hard` · `git push --force` · 파일 대량 삭제는 **명시 승인 전에 절대 실행하지 않는다.**

### 작업을 마칠 때
- [테스트 · 검증](docs/agents/testing.md) 의 명령을 **실제로 돌린 뒤에** 완료를 보고한다. 실패하면 출력 그대로 알린다.
- 구조·명령·설정이 바뀌었으면 문서 갱신까지가 그 작업의 일부다 — `README.md` 와 `docs/agents/` 양쪽.

### 서버 실행
- 개발 서버는 `./dev.sh` 또는 `.claude/launch.json` 의 preview 구성을 쓴다(백엔드 8010 · 프론트 5173).
- 이미 떠 있는 포트에 새로 띄우지 않는다. 먼저 확인한다.
